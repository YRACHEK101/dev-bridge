node_modules/
dist/
*.log
.DS_Store
coverage/

# dev-bridge generated config is a project artifact; keep it if you commit it,
# but ignore local env files.
.env
.env.local

# --- AI Dev Team local tooling (never commit) ---
ai-dev-team/
